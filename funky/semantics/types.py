"""Built-in types available for use in Funky."""

from enum import Enum, auto

class Primitives(Enum):
    """Primitive types -- types which cannot be """
    INTEGER  =  auto()
    FLOAT    =  auto()
    CHAR     =  auto()
