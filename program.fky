module test where

    otherwise = True

    newcons List = Cons Integer List | Nil

    concatenate (Cons x xs) ys = Cons x (xs `concatenate` ys)
    concatenate Nil         ys  = ys
    concatenate xs          Nil = xs

    range m n | m == n    = Cons m Nil
              | otherwise = Cons m (range (m + 1) n)

    main = range m n
           where m = 1
                 n = 100
