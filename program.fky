module test where

    newcons Tree = Branch Tree Integer Tree | Leaf Integer
    newcons List = Cons Integer List | Nil

    drop 0 (Cons a b) = Cons a b
    drop n (Cons a b) = drop (n - 1) b
    drop n Nil        = Nil

    tail = drop 1

    main = tail
