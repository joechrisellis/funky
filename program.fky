module test where

    newcons List = Cons Integer List | Nil
    newcons Tree = Branch Tree Integer Tree | Leaf Integer

    concatenate (Cons a b) x = Cons a (concatenate b x)
    concatenate Nil        x = x

    main = concatenate
