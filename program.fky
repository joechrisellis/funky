module test where

    newtype Test = Integer
    newcons List = Cons Test List | Nil

    index e (Cons x xs) | e == x    = 0
                        | otherwise = 1 + index e xs

    otherwise = True

    main = index
